Запусти все тесты в проекте стандартной командой Pytest.

Выполни тесты только из одного указанного файла.

Запусти один конкретный тест по его имени.

Выполни тесты с подробным выводом в консоль.

Запусти тесты так, чтобы отображались все сообщения print().

Проверь, как меняется формат ошибок при использовании разных вариантов параметра --tb.

Запусти тесты с отображением логов уровня INFO в консоли.

Сохрани все логи выполнения тестов в файл.

Пометь тесты меткой @pytest.mark.smoke и запусти только их.

Запусти все тесты, кроме отмеченных меткой smoke.

Выполни тесты параллельно в нескольких потоках.

Сформируй отчёт о тестах в формате JUnit XML.

Сгенерируй HTML-отчёт с результатами выполнения тестов.

Проведи запуск тестов с ограничением количества допустимых ошибок.

Проверь возможность комбинированного запуска тестов с фильтрацией по имени и метке.

Выполни тесты в тихом режиме без лишней информации в консоли.

Запусти тесты с максимальным уровнем детализации (-vv).

Проверь создание отчёта с логами и результатами тестов в одном HTML-файле.

Отключи захват вывода полностью и проанализируй, как отображаются сообщения в консоли.

Настрой запуск тестов с одновременным выводом логов и сохранением их в файл.